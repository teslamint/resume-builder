name: CI

on:
  push:
    branches: [main]
    paths:
      - 'templates/**'
      - 'example/**'
      - 'build.sh'
      - 'Dockerfile'
      - 'docker-compose.yml'
      - 'Makefile'
      - '.github/workflows/**'
  pull_request:
    branches: [main]
    paths:
      - 'templates/**'
      - 'example/**'
      - 'build.sh'
      - 'Dockerfile'
      - 'docker-compose.yml'
      - 'Makefile'
      - '.github/workflows/**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install test dependencies
        run: pip install pytest pyyaml

      - name: Run unit tests
        working-directory: templates
        run: python3 -m pytest tests/ -v

      - name: Validate schema
        run: python3 templates/build/schema.py --validate-all --example

  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y pandoc weasyprint

      - name: Build example resume
        run: ./build.sh example all

      - name: Build example career description
        run: ./build.sh example career

      - name: Verify outputs
        run: |
          test -f private/build/resume-example.md
          test -f private/build/resume-example.pdf
          test -f private/build/resume-example-short.pdf
          test -f private/build/resume-example-wanted.txt
          test -f private/build/career-description-example.pdf
          echo "✅ All outputs generated successfully"

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: resume-example
          path: |
            private/build/resume-example*
            private/build/career-description-example*
          retention-days: 7

  docker-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build Docker image
        run: docker compose build

      - name: Build example resume in Docker
        run: docker compose run --rm resume example all

      - name: Build example career description in Docker
        run: docker compose run --rm resume example career

      - name: Verify outputs
        run: |
          test -f private/build/resume-example.md
          test -f private/build/resume-example.pdf
          test -f private/build/resume-example-short.pdf
          test -f private/build/resume-example-wanted.txt
          test -f private/build/career-description-example.pdf
          echo "✅ Docker build outputs generated successfully"
